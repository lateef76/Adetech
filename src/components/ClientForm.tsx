/**
 * DEPRECATED: ClientForm Component
 *
 * This component is no longer used and has been deprecated.
 *
 * Client management is now handled via:
 * - NewReceiptForm: Manual client entry with automatic creation
 * - AddClientDialog & EditClientDialog: For client management
 *
 * Stub kept to prevent import errors.
 */

export function ClientForm() {
  throw new Error(
    "ClientForm is deprecated. Use NewReceiptForm or AddClientDialog/EditClientDialog instead.",
  );
}
